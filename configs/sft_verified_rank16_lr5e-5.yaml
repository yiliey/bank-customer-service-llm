### Verified from the saved LLaMA-Factory training_args.yaml.
### Paths were changed from AutoDL absolute paths to repository-relative paths.

### model
model_name_or_path: Qwen/Qwen2.5-3B-Instruct
trust_remote_code: true

### method
stage: sft
do_train: true
finetuning_type: lora
lora_rank: 16
lora_alpha: 32
lora_dropout: 0.05
lora_target: all
loraplus_lr_ratio: 16

### dataset
dataset: banking77_sft
dataset_dir: data
template: qwen
cutoff_len: 1024
val_size: 0.1
max_samples: 100000
preprocessing_num_workers: 16
packing: false

### output
output_dir: outputs/sft-rank16-lr5e-5
logging_steps: 5
save_steps: 100
eval_strategy: steps
eval_steps: 100
plot_loss: true
report_to: none

### train
per_device_train_batch_size: 2
per_device_eval_batch_size: 2
gradient_accumulation_steps: 8
learning_rate: 5.0e-5
num_train_epochs: 6.0
lr_scheduler_type: cosine
warmup_steps: 0
max_grad_norm: 1.0
optim: adamw_torch
bf16: true
flash_attn: auto
ddp_timeout: 180000000

